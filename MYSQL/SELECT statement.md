Select statement is the statement that is used to select the specific columns and rows of data from the database

can be used like:


```
SELECT * #* means all
FROM (what data base you are using).(what tables u are using);
```

